{
  "_comment": "Per-IG overrides for publication-request.json generation. Keyed by ImplementationGuide.packageId (or .id). Any field set here wins over the value derived from the ImplementationGuide resource. Supported keys per entry: package-id, version, path, mode, status, sequence, descmd, desc, changes, canonical. Leave 'overrides' empty to rely entirely on the generic derivation.",
  "overrides": {}
}
